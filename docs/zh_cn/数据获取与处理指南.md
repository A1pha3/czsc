# CZSC 数据获取与处理指南

> 本指南介绍 CZSC 数据获取、转换和处理的完整流程

> **版本兼容性**：本文档基于 CZSC v0.10.x 编写，需要 Python >= 3.10。
> 数据客户端 `DataClient` 支持 Tushare 数据源，其他数据源通过连接器模块接入。

> **前置知识**：建议先完成 [快速开始指南](./快速开始指南.md)，理解 `RawBar`、`Freq` 等基础数据结构。

## 学习目标

完成本章学习后，你将能够：

### 基础目标（必掌握）

- [ ] 理解 DataClient 统一数据接口
- [ ] 掌握多数据源的使用方法
- [ ] 理解 K 线数据格式转换
- [ ] 能够使用 BarGenerator 合成多周期K线

### 进阶目标（建议掌握）

- [ ] 掌握数据缓存机制
- [ ] 理解数据校验和清洗方法
- [ ] 能够处理异常数据情况

### 专家目标（挑战）

- [ ] 设计自定义数据源连接器
- [ ] 实现数据质量监控系统
- [ ] 优化数据获取性能

---

## 一、数据接口概述

### 1.0 数据处理全链路

> 理解全链路有助于快速定位问题出在哪个环节。

```
数据源               格式转换               分析
───────────         ──────────          ──────────
Tushare      ─┐
聚宽 SDK      ├──→  DataFrame  ──→  format_standard_kline()  ──→  List[RawBar]
CCXT/天勤     ─┤                                                      │
Mock数据      ─┘                                                      ▼
                                                              CZSC(bars) → 分型/笔/中枢
                                                                    │
                                                                    ▼
                                                           BarGenerator → 多周期合成
                                                                    │
                                                                    ▼
                                                           CzscSignals → 信号计算
```

### 1.1 DataClient 统一接口

```python
from czsc.utils import DataClient

# 创建数据客户端
dc = DataClient(
    token="your_token",              # API凭证
    url="http://api.tushare.pro",    # API地址
    timeout=300,                     # 请求超时时间
    verbose=True                     # 显示详细日志
)

# 获取K线数据
df = dc.get_kline(
    symbol="000001",    # 标的代码
    freq="日线",        # K线周期
    sdt="20230101",     # 开始日期
    edt="20231231",     # 结束日期
    fq="前复权"         # 复权方式
)
```

### 1.2 支持的数据源

| 数据源 | 标识 | 支持市场 | 数据类型 | 说明 |
|--------|------|----------|----------|------|
| **Tushare** | `ts` | A股 | 日线、分钟线 | 需要token |
| **聚宽** | `jq` | A股 | 日线、分钟线 | 需要账户 |
| **天勤** | `tq` | 期货 | Tick、分钟线 | 免费实时 |
| **CCXT** | `ccxt` | 数字货币 | Tick、K线 | 多交易所 |
| **本地文件** | `file` | - | CSV/Feather | 本地数据 |

---

## 二、数据获取方法

### 2.1 Tushare 数据源

```python
from czsc.utils import DataClient

# 初始化Tushare客户端
dc = DataClient(
    token="your_tushare_token",
    url="http://api.tushare.pro"
)

# 获取日线数据
df_daily = dc.get_kline(
    symbol="000001.SZ",
    freq="日线",
    sdt="20230101",
    edt="20231231",
    fq="前复权"
)

# 获取分钟数据
df_1m = dc.get_kline(
    symbol="000001.SZ",
    freq="1分钟",
    sdt="20240101",
    edt="20240131"
)

# 获取股票列表
stock_list = dc.get_stock_list()
```

### 2.2 聚宽数据源

> **注意**：聚宽连接器目前不包含在默认安装中，需要根据聚宽 SDK 自行接入。
> 可以通过 `DataClient` 封装聚宽接口，或编写自定义连接器函数。

```python
# 推荐方式：通过自定义 read_bars 函数封装聚宽接口
def read_bars_from_jq(symbol, freq, sdt, edt, fq='后复权'):
    """从聚宽获取K线数据
    
    需要先安装并认证聚宽 SDK：pip install jqdatasdk
    """
    import jqdatasdk as jq
    jq.auth('your_username', 'your_password')
    
    df = jq.get_bars(symbol, count=None, unit=freq,
                     fields=['date', 'open', 'close', 'high', 'low', 'volume', 'money'],
                     include_now=False, end_dt=edt)
    # 标准化列名
    df = df.rename(columns={'date': 'dt', 'volume': 'vol', 'money': 'amount'})
    return df
```

### 2.3 CCXT 数字货币

> **注意**：`ccxt_connector` 模块提供的是函数式 API，而非类实例化接口。

```python
from czsc.connectors.ccxt_connector import get_symbols

# 获取交易所支持的交易对列表
symbols = get_symbols(exchange="binance")

# 如需获取K线数据，可参考以下方式：
import ccxt
exchange = ccxt.binance()
ohlcv = exchange.fetch_ohlcv('BTC/USDT', timeframe='1h', limit=500)

# 转换为DataFrame后使用 format_standard_kline
import pandas as pd
df = pd.DataFrame(ohlcv, columns=['dt', 'open', 'high', 'low', 'close', 'vol'])
df['dt'] = pd.to_datetime(df['dt'], unit='ms')
df['amount'] = df['vol'] * df['close']  # 近似成交额
```

### 2.4 天勤期货数据

```python
from czsc.connectors.tq_connector import TqConnector

# 初始化天勤客户端
tq_dc = TqConnector()

# 获取期货数据
df = tq_dc.get_kline(
    symbol="SHFE.cu2405",
    freq="1分钟",
    sdt="20240101",
    edt="20240131"
)
```

---

## 三、数据格式转换

### 3.1 DataFrame 转 RawBar

```python
from czsc.core import format_standard_kline, Freq

# 标准数据格式
required_columns = [
    'dt',      # 日期时间
    'open',    # 开盘价
    'close',   # 收盘价
    'high',    # 最高价
    'low',     # 最低价
    'vol',     # 成交量
    'amount'   # 成交额
]

# 确保数据列名正确
df = df.rename(columns={
    'trade_date': 'dt',
    'volume': 'vol',
    'amount': 'amount'
})

# 转换时间格式
df['dt'] = pd.to_datetime(df['dt'])

# 转换为 RawBar 对象
bars = format_standard_kline(df, freq=Freq.F30)

# 查看结果
print(f"转换后K线数量: {len(bars)}")
print(f"第一根K线: {bars[0]}")
```

### 3.2 RawBar 转 DataFrame

```python
from czsc.core import RawBar
import pandas as pd

def bars_to_dataframe(bars: list) -> pd.DataFrame:
    """将 RawBar 列表转换为 DataFrame

    参数：
        bars: RawBar 对象列表

    返回：
        DataFrame
    """
    data = [{
        'symbol': bar.symbol,
        'dt': bar.dt,
        'id': bar.id,
        'open': bar.open,
        'close': bar.close,
        'high': bar.high,
        'low': bar.low,
        'vol': bar.vol,
        'amount': bar.amount,
    } for bar in bars]

    return pd.DataFrame(data)

# 使用
df = bars_to_dataframe(bars)
```

---

## 四、BarGenerator K线合成

### 4.1 基础用法

```python
from czsc.core import BarGenerator

# 创建K线生成器
bg = BarGenerator(
    base_freq='1分钟',    # 基础周期
    freqs=['5分钟', '15分钟', '30分钟', '日线']  # 目标周期
)

# 输入基础周期K线
for bar in bars_1m:
    bg.update(bar)

# 获取合成后的K线
bars_5m = bg.bars['5分钟']
bars_30m = bg.bars['30分钟']
bars_daily = bg.bars['日线']

print(f"1分钟: {len(bars_1m)} 根")
print(f"5分钟: {len(bars_5m)} 根")
print(f"30分钟: {len(bars_30m)} 根")
print(f"日线: {len(bars_daily)} 根")
```

### 4.2 自定义周期

```python
# 支持的周期类型
from czsc.py.enum import Freq

# 分钟周期
F1 = Freq.F1      # 1分钟
F5 = Freq.F5      # 5分钟
F15 = Freq.F15    # 15分钟
F30 = Freq.F30    # 30分钟
F60 = Freq.F60    # 60分钟

# 日周期
FD = Freq.FD      # 日线
FW = Freq.FW      # 周线
FM = Freq.FM      # 月线

# 自定义周期
bg = BarGenerator(
    base_freq='1分钟',
    freqs=['3分钟', '7分钟', '2小时'],  # 自定义周期
)
```

### 4.3 市场类型处理

```python
from czsc.py.bar_generator import check_freq_and_market

# 检查频率与市场是否匹配
is_valid, msg = check_freq_and_market(
    freq='日线',
    market='stock'  # stock/future/crypto
)

# 获取交易日内的时间切分
from czsc.py.bar_generator import get_intraday_times

# A股日内时间点
times_stock = get_intraday_times(market='A股')

# 期货日内时间点
times_future = get_intraday_times(market='期货')
```

---

## 五、数据缓存机制

### 5.1 缓存概述

```python
"""
CZSC 使用本地缓存减少重复数据请求：

缓存位置：~/.quant_data_cache/
缓存方式：pickle 格式
缓存键：参数的MD5哈希值
"""
```

### 5.2 缓存管理

```python
from czsc.utils import home_path, get_dir_size, empty_cache_path
import os

# 查看缓存目录
cache_dir = home_path
print(f"缓存目录: {cache_dir}")

# 查看缓存大小
size_mb = get_dir_size(cache_dir) / 1024 / 1024
print(f"缓存大小: {size_mb:.2f} MB")

# 清空缓存
empty_cache_path()
print("缓存已清空")

# 清理过期缓存（默认30天前）
from czsc import clear_expired_cache
from czsc.utils import home_path
clear_expired_cache(home_path, max_age=3600 * 24 * 30)  # path 必选，max_age 单位为秒
```

### 5.3 缓存配置

```python
from czsc.utils import DataClient

# 自定义缓存路径
dc = DataClient(
    token="your_token",
    cache_path="/custom/cache/path"  # 自定义缓存目录
)

# 启用时不使用缓存
dc = DataClient(
    token="your_token",
    clear_cache=True  # 初始化时清空缓存
)
```

---

## 六、数据校验与清洗

### 6.1 数据校验

```python
from czsc.utils.data.validators import (
    validate_dataframe_columns,
    validate_datetime_index,
    validate_numeric_column,
    validate_date_range,
    validate_no_duplicates,
)

# 校验K线数据：逐项检查
# 1. 检查必需列是否存在
required = ['dt', 'open', 'close', 'high', 'low', 'vol', 'amount']
validate_dataframe_columns(df, required)

# 2. 检查时间列
validate_datetime_index(df, col='dt')

# 3. 检查数值列
for col in ['open', 'close', 'high', 'low']:
    validate_numeric_column(df, col)

# 4. 检查日期范围
validate_date_range(df, col='dt', start='20230101', end='20231231')

# 5. 检查重复记录
validate_no_duplicates(df, subset=['dt'])

print("所有校验通过")
```

### 6.2 数据清洗

```python
import pandas as pd
import numpy as np

def clean_kline_data(df):
    """清洗K线数据

    参数：
        df: 原始K线数据

    返回：
        清洗后的数据
    """
    df = df.copy()

    # 1. 处理缺失值
    df = df.dropna(subset=['open', 'close', 'high', 'low'])

    # 2. 处理异常值
    # 价格必须为正
    for col in ['open', 'close', 'high', 'low']:
        df = df[df[col] > 0]

    # 成交量不能为负
    df = df[df['vol'] >= 0]

    # 3. 修正高低点关系
    df['high'] = df[['open', 'close', 'high']].max(axis=1)
    df['low'] = df[['open', 'close', 'low']].min(axis=1)

    # 4. 去除重复
    df = df.drop_duplicates(subset=['dt'])

    # 5. 排序
    df = df.sort_values('dt').reset_index(drop=True)

    return df

# 使用
df_clean = clean_kline_data(df_raw)
```

### 6.3 异常检测

```python
def detect_anomalies(df):
    """检测数据异常

    返回异常记录
    """
    anomalies = []

    # 检测价格跳空（超过20%）
    df['prev_close'] = df['close'].shift(1)
    df['gap'] = abs(df['open'] / df['prev_close'] - 1)
    gap_anomalies = df[df['gap'] > 0.2]

    # 检测成交异常（超过均值10倍）
    vol_mean = df['vol'].mean()
    vol_anomalies = df[df['vol'] > vol_mean * 10]

    # 检测停牌（无成交）
    zero_vol = df[df['vol'] == 0]

    return {
        '跳空异常': gap_anomalies,
        '成交异常': vol_anomalies,
        '停牌': zero_vol
    }
```

---

## 七、Mock 数据生成

### 7.1 基础用法

```python
from czsc.mock import generate_symbol_kines

# 生成模拟数据
df = generate_symbol_kines(
    symbol='000001',        # 标的代码
    freq='30分钟',          # K线周期
    sdt='20240101',         # 开始日期
    edt='20240131',         # 结束日期
    seed=42                 # 随机种子（可重现）
)

print(f"生成数据: {df.shape}")
print(df.head())
```

### 7.2 多品种生成

```python
# 批量生成多个品种数据
symbols = ['000001', '000002', '600000', '600036']
freq = '30分钟'
sdt = '20240101'
edt = '20240131'

data = {}
for symbol in symbols:
    df = generate_symbol_kines(symbol, freq, sdt, edt, seed=len(symbol))
    data[symbol] = df
```

### 7.3 自定义数据特征

```python
# 生成模拟数据（通过 seed 控制随机性，不同 seed 产生不同走势）
df_trending = generate_symbol_kines(
    symbol='000001',
    freq='日线',
    sdt='20230101',
    edt='20231231',
    seed=42              # 随机种子，控制数据可重现性
)

# 注意：generate_symbol_kines 的完整签名为：
# generate_symbol_kines(symbol, freq, sdt="20100101", edt="20250101", seed=42)
# 不支持 trend_type、volatility 等参数
```

---

## 八、常见问题

### Q1: 数据缺失怎么办？

A: 使用前向填充或线性插值：

```python
# 检测缺失日期
all_dates = pd.date_range(start=df['dt'].min(), end=df['dt'].max(), freq='D')
missing_dates = set(all_dates) - set(df['dt'])

# 前向填充
df = df.set_index('dt').asfreq('D', method='ffill').reset_index()

# 线性插值
df[['open', 'close', 'high', 'low']] = df[['open', 'close', 'high', 'low']].interpolate()
```

### Q2: 如何处理停牌数据？

A: 识别停牌并跳过处理：

```python
# 检测停牌（成交量为0）
suspended = df[df['vol'] == 0].copy()

# 策略中跳过停牌日
def should_trade(dt, suspended_dates):
    if dt in suspended_dates:
        return False
    return True
```

### Q3: 数据速度慢怎么办？

A: 使用缓存和批量请求：

```python
# 1. 启用缓存
dc = DataClient(token=token, cache_path="./cache")

# 2. 批量获取
symbols = ['000001', '000002', '600000']
for symbol in symbols:
    df = dc.get_kline(symbol, '日线', '20230101', '20231231')
    # 第一次慢，后续快（使用缓存）
```

---

## 九、最佳实践

### 9.1 数据管理

```python
"""
数据管理最佳实践：

1. 统一数据源
   - 使用 DataClient 统一接口
   - 配置标准数据格式

2. 缓存策略
   - 合理设置缓存过期时间
   - 定期清理无用缓存

3. 数据备份
   - 重要数据本地备份
   - 使用版本控制

4. 数据监控
   - 监控数据质量
   - 及时发现异常
"""
```

### 9.2 数据处理流程

```python
"""
标准数据处理流程：

1. 获取原始数据
   └── DataClient.get_kline()

2. 数据校验
   ├── 格式校验
   ├── 完整性校验
   └── 合理性校验

3. 数据清洗
   ├── 处理缺失值
   ├── 处理异常值
   └── 修正错误数据

4. 数据转换
   └── format_standard_kline()

5. 数据缓存
   └── 保存到本地缓存
"""
```

---

## 十、练习与巩固

### 练习 1：数据获取

使用 DataClient 获取一个完整月的日线数据，并进行基本统计分析。

### 练习 2：数据清洗

编写一个函数，检测并修复K线数据中的常见问题。

### 练习 3：多周期合成

使用 BarGenerator 从1分钟K线合成多个周期，并验证正确性。

---

## 十一、进阶学习

完成本章后，建议继续学习：

1. **[核心概念详解](./核心概念详解.md)** - 理解数据使用
2. **[可视化工具使用指南](./可视化工具使用指南.md)** - 学习数据可视化
3. **[回测框架使用指南](./回测框架使用指南.md)** - 学习数据应用

---

**文档版本**: v1.0.0
**最后更新**: 2026-02-28
**维护**: CZSC Community
